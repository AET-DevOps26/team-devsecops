---
# Internal only — not exposed via ingress. Only spring-api calls this service.
apiVersion: v1
kind: Service
metadata:
  name: py-recipe-service
  namespace: app
spec:
  selector:
    app: py-recipe-service
  ports:
    - port: 8080
      targetPort: 8080
