---
# Template — NEVER commit real values.
# INTERNAL_AUTH_SECRET must be the SAME value as in spring-api-secret.
# See spring-api/secret.yaml for the generation commands.
apiVersion: v1
kind: Secret
metadata:
  name: py-recipe-secret
  namespace: app
type: Opaque
stringData:
  SERVICE_API_KEY: REPLACE_WITH_GEMINI_API_KEY
  INTERNAL_AUTH_SECRET: REPLACE_WITH_HMAC_SECRET  # same as spring-api-secret
